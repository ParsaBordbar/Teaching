#include <stdio.h>
#include <stdlib.h>

#define PI 3.14159265359

int main(){  

    /*When we want others (or yourself)
    to not change the existing variable values, you
    can use the const keyword.*/

    const char myFavoriteChar = 'M';
    // pi = 3; can not be Done!

    /*When you declare a constant variable,
     it must be assigned with a value

    It is a good practice to name Constants
    with UpperCase letters */

    const char MYFAVORITECHAR = 'M';
    
return 0;
}