#include <stdio.h>
#include <stdlib.h>

int main(){  

    int i = 0;
// What is a loop? a loop is a code block that repeats!
    while (i < 10) {
    printf("This is the %d Line \n", i);
    i++;
    }

    return 0;
}